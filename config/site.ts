export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "travel-planner",
  description: "Under construction.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ]
};
